//#include<stdio.h>
//int main(){
//	int v[100]={0},i,n=10,m=4,count=1,out=0;
//	scanf("%d%d",&n,&m);
//	for(i=1;i<=n;i++){
//	v[i]=1;
//	}
//	i=1;
//	while(out<n)
//	{	
//	if(m==count&&v[i]!=0)
//	{v[i]=0;
//	printf("%d ",i);
//	i++;
//	count=1;
//	out++;
//	}
//	if(v[i]!=0)
//	count++;
//	i++;		
//	if(i>n)
//	i=1;	
//	
//	
//	
//	}
//	
//}
//
//	/*while(1){
//		for(i=1;i<=m;i++){
//		if(*p==0)
//		p++;
//		p++;
//		}		
//	   point=p-(v+9);
//		if(point>=0)
//			p=v+point;
//	
//	*p=0;
//	for(i=1,k=0;i<=n;i++){
//		if(v[i]==1)
//	      k++;}
//	
//	index=p-(v+1);
//	if(index<=9&&index>0)
//	printf("%d",index);
//	if(k==1)
//		break;*/
//	
//	/*}*/
//
//
////}
//
////#include <stdio.h>
////int main()
////{
////    int n, m; // n为人数，m为间隔数
////    scanf("%d%d", &n, &m); // 输入n和m
////    int v[100]; // 定义一个大小为n的数组v，用来表示每个人是否出列，0表示在圈内，1表示出列
////    for (int i = 0; i < n; i++) // 初始化数组v，所有元素都为0
////    {
////        v[i] = 0;
////    }
////    int p = 0; // 定义一个指针p，用来遍历数组v
////    int count = 0; // 定义一个计数器count，用来记录报数
////    int left = n; // 定义一个变量left，用来记录还剩下多少人在圈内
////    while (left > 0) // 当还有人在圈内时，循环执行以下操作
////    {
////        if (v[p] == 0) // 如果p指向的元素为0，表示该位置还在圈内
////        {
////            count++; // 报数加一
////            if (count == m) // 如果报数等于m，表示该位置要出列
////            {
////                v[p] = 1; // 将该位置置为1，表示出列
////                printf("%d ", p + 1); // 输出该位置的编号（从1开始）
////                count = 0; // 报数归零
////                left--; // 剩余人数减一
////            }
////        }
////        p++; // 指针p向后移动一个位置
////        if (p == n) // 如果p到达数组末尾，回到数组开头继续遍历（模拟循环）
////        {
////            p = 0;
////        }
////    }
////    printf("\n"); // 输出换行符结束程序
////
////}
//
